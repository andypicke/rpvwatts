default_params <- function(){
  # set defaults for required parameters
  req_defaults <- list(system_capacity = 4,
                       module_type = 0,
                       losses = 14,
                       array_type = 1,
                       tilt = 30,
                       azimuth = 180,
                       address = "Denver,co",
                       timeframe = "hourly")
}
